- This project is a Simple XML DBMS .
- This project was done by the team of:
   (Mostafa Tarek Ibrahim - Omar Ahmed Foad- Sherif Mohammed Abd-alrahman - Shabab Sheta)
- To know the syntax of SQL you need to visit w3schools page http://www.w3schools.com/sql/.
*- The Main function is in src/Controller/Main
**- You Have to import the jar files 'for the json files' into the Referenced Libraries by:
    In your Eclipse environment :
    1-Right click on the project folder i.e SQL
    2-click Build Path
    3-click Add External Archives
    4-select the jar files
- Example:
	SQL>>create database db;
	Data Base Created
	Syntax is valid
	
	SQL>>use database db;
	Syntax is valid
	
	SQL>>create table travel (name varchar , city varchar , country varchar);
	Table Created
	Syntax is valid
	
	SQL>>insert into travel (name, city, country) values (Sherif, Alex, Egy);
	Syntax is valid
	
	SQL>>insert into travel (name, city, country) values (Mohammed, Cairo, Egy);
	Syntax is valid
	
	SQL>>select * from travel;
	+----------+-------+---------+
	| name     | city  | country |
	+----------+-------+---------+
	| Sherif   | Alex  | Egy     |
	| Mohammed | Cairo | Egy     |
	+----------+-------+---------+
	Syntax is valid
	
	SQL>>select * from travel where city='Alex';
	+--------+------+---------+
	| name   | city | country |
	+--------+------+---------+
	| Sherif | Alex | Egy     |
	+--------+------+---------+
	Syntax is valid
	
	SQL>>
  